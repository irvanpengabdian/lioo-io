# Salin ke .env di folder ini (atau set di root monorepo, sesuai kebiasaan tim).
# Semua app yang memakai @repo/database berbagi schema PostgreSQL yang sama.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
