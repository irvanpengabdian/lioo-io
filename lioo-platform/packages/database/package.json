{
  "name": "@repo/database",
  "version": "0.0.0",
  "private": true,
  "main": "index.ts",
  "prisma": {
    "seed": "npx tsx seed-pos-menu.ts"
  },
  "scripts": {
    "postinstall": "prisma generate",
    "build": "prisma generate",
    "db:generate": "prisma generate",
    "db:validate": "prisma validate",
    "db:format": "prisma format",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:verify:order-schema": "node scripts/verify-order-delivery-urls.mjs",
    "db:migrate:status": "prisma migrate status",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:seed:pos": "npx tsx seed-pos-menu.ts",
    "db:sync-env-from-pos": "node scripts/sync-database-url-from-pos.mjs",
    "db:migrate:deploy:pos": "node scripts/migrate-with-pos-env.mjs",
    "db:seed:pos:from-pos": "node scripts/seed-with-pos-env.mjs"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.0.0"
  }
}
